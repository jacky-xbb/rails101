<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <%= link_to 'Rails 101', root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <% if current_user %>
                    <li class="nav-item dropdown">
                        <%= link_to "#", class: 'nav-link dropdown-toggle', id: 'userDropdown', role: 'button', 'data-bs-toggle': 'dropdown', 'aria-expanded': 'false' do %>
                            Hi!, <%= current_user.email %>
                        <% end %>
                        <ul class="dropdown-menu" aria-labelledby="userDropdown">
                            <li><%= link_to "My Groups", account_groups_path, class: 'dropdown-item' %></li>
                            <li><%= link_to "My Posts", account_posts_path, class: 'dropdown-item' %></li>
                            <hr class="dropdown-divider">
                            <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
                        </ul>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to 'Login', new_user_session_path, class: 'nav-link' %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Signup', new_user_registration_path, class: 'nav-link' %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
